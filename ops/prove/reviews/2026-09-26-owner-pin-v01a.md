# PROVE — owner-pin v0.1a format and trust review

Reviewed exact design commit `924dc9014b56f312ba5073b27131bec58fb5dd32`. The re-fetched unsigned JSON Schema blob is `6874400d2f8576db4d91d110c87dbdb2a0f5f5d9`.

## Independently reproduced structural tests

GitHub did not resolve from the isolated runtime, so the schema was reconstructed locally from the connector-fetched exact committed file and checked with `git hash-object`: **the local copy matched the Git blob byte-for-byte**. A fresh Python `jsonschema` 4.26.0 Draft 2020-12 validator with format checking passed **19/19** structural acceptance/rejection cases. Cases included the valid unsigned mock payload; missing signer fingerprint; wrong scope/policy; indefinite or malformed expiry; undeclared field; formerly accepted dot/traversal/trailing-slash manifest paths; duplicate/reordered/traversal checker paths; third checker file; invalid repository/provider/tree digest; UUID and SHA format.

The v0.1 generic path regex was independently challenged before this revision and incorrectly accepted the bare `..`, `.`, `foo/..`, `foo/.`, `foo//bar` and `foo/`. v0.1a eliminates these ambiguous paths by fixing the only required manifest and two checker paths. The old v0.1 unsigned example must have its `schemaVersion` replaced with `nulvr.owner-pin-design.v0.1a` before reuse as a v0.1a mock; all other fields match the narrowed shape.

## Remaining architecture constraints

**Schema result: PASS as an untrusted DESIGN FORMAT only. Cryptographic/protected attestation: NOT PASS and NOT IMPLEMENTED.**

- Define a v0.1a-specific domain separator, `NULVR:OWNER-PIN:V0.1A\n`, instead of silently reusing the v0.1 protocol string. Verify canonical RFC8785 payload bytes and reject duplicate JSON keys before signature validation.
- Authorized founder public-key fingerprint, key custody, rotation/revocation and publishing permissions must be independently controlled, outside BUILD/PROVE and the same work-branch record. The current main ruleset requires one approval but **does not require a founder-only reviewer or founder signature**.
- Source/evidence Git commit+tree IDs require provider validation, raw-byte per-file SHA256 checks and private evidence access; unsigned mock digests are not evidence.
- JSON Schema cannot enforce temporal ordering, key ownership, actual Git reachability, immutable object retention or verification-ID replay state. An independent durable ID registry is required before one-shot semantics; do not imply it exists.
- Public NULVR OPS must not store private ALPS evidence. A separately approved protected VERIFIED writer and remote/production ALPS qualification remain different gates.

## Disposition

**DESIGN-SCHEMA SOURCE-BOUND TESTS: PASS (19/19); v0.1a signature and protected-anchor design: CANDIDATE, founder T2 GO PENDING.** Stage source-reviewed schema on the company branch only as non-executable design if desired; do not deploy a signer, create an actual approval record or enable release authority.

No CI, paid run, PR, main merge, secrets, owner signature, protected-ledger write, deploy or production probe occurred.
