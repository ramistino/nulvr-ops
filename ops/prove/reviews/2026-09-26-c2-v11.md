# PROVE — C2 v1.1 exact-source handoff schema review

**Reviewed source commit:** `334731390a1fe17579f4626a0e805c27bcf4e77a`. **Exact new schema Git blob:** `623f6c72fb277f8fbc49cd43853ee0885ceea65c`, independently re-fetched via GitHub. Historical v1 format blob: `4ebaf63decffc3b51a75d5c9865a6a6c39c0df9d`; original example blob: `b6c78f195032121aa72debfd70629703db2bb18d`.

Fresh local Python `jsonschema` Draft 2020-12 source-bound recheck verified an isolated schema copy's exact Git blob. **17/17 expected acceptance/rejection cases passed.** Existing C2 example remains valid; all six untrusted states are accepted; five privileged/assertion statuses (`VERIFIED`, `REFUTED`, `RELEASED`, `ASSERTION_PASS`, `ASSERTION_FAIL`) are rejected; missing `authority_required`, invalid source SHA, unknown scope and unknown authority are rejected.

**Source review result:** PASS for **format-only untrusted handoffs**. This closes the previously demonstrated v1 status-injection ambiguity *for records explicitly validated against v1.1*. Historical v1 remains unchanged and cannot be treated as an approval gate. The new schema itself does not check actor identity, approve evidence, prove a source commit or authorize release; `authority_required` is a workflow declaration only.

**Adoption recommendation:** copy the exact reviewed v1.1 schema + notes into `ops/company-track-v1`, update its README to name v1.1 as the active handoff format and retain v1.0 as historical. Record C2 as company-branch source-reviewed rather than main-deployed or trusted automated governance. Do not trigger the unresolved PR #7 Actions workflow or activate a protected verdict writer.

This review is a fresh isolated technical schema validation; it is not a separately credentialed runtime PROVE service.
