# PROVE/GUARD — C4 v0.1 hardened source review

**Reviewed commit:** `182174a243f1730ef124f22b1c3e01481d3288ff`. **Mode:** exact Git source review only. BUILD's two 50/50 local runs are recorded evidence from BUILD and are not converted here into an independent runtime rerun.

## Exact reviewed objects

- harness: `5ae20aad76243799ff76f6893f22aa792a6be713`
- tests: `45c97258eaabd90dff63365898aad4439e452707`
- unchanged local target: `9d93e485e449ffccfbc262d5abf65aca55d08c24`
- approval record: `c72d824ca77d7f4e17f95607e2aa811a35752dff`
- BUILD validation note: `f4926912191c95de47b7560248cc0c4bb3e77cd0`

## Findings

**PASS — prior review gaps G1–G3 are materially addressed in source.**
- Config/fixture use descriptor-based `limit+1` bounded reads with `O_NOFOLLOW` when available and before/after descriptor metadata checks. Limits are 4 KiB and 64 KiB.
- Config and fixture must be direct children of one canonical owner-only local root; symlink files/root and fixture path escape are rejected.
- Watchdog result model separates `HTTP_200`, `HTTP_NON_200`, `TIMEOUT`, and `TRANSPORT_ERROR`; every non-200 category contributes to `livenessFailed`.
- Successful sample p50/p95/p99 are explicitly separate from failure counts.
- Result states `fixtureUsedAsWorkload:false`, correctly preventing the fixture digest from being presented as workload replay.
- Target remains fixed to the bundled localhost child; config has no endpoint field. No cloud provisioning, ALPS URL, ledger writer, deployment or billing path was introduced.

**PARTIAL — resource/provenance limits remain explicit rather than falsely solved.**
- 96 MiB V8 old-space is not an OS RSS/CPU quota; output records observations only.
- `pinnedCommit` remains a syntax-validated claim, not protected owner attestation.
- Tool hash is self-reported and needs independent source binding for trusted use.
- Mutable ancestor-directory / enumeration-to-open races are not a hostile-filesystem guarantee.

## Test-evidence distinction

BUILD records two offline executions of the full suite: **50/50 + 50/50** on Node v22.16.0 with separate raw log digests. This review confirms that the committed test source includes eight C4 hardening tests, but does **not** independently reproduce those 50/50 runs.

One acceptance item from the lock is still only unit-simulated: transport-error and HTTP-non-200 classification are tested by pure function input, not by an end-to-end deliberately closed local socket / 503 child response. This does not invalidate the source hardening, but it blocks calling the watchdog failure-injection matrix complete.

## GUARD disposition

**SOURCE REVIEW PASS / INDEPENDENT RUNTIME + E2E FAILURE INJECTION PENDING.**

No widening of authority is justified. Keep C4 localhost synthetic only. Before operational qualification:
1. independently rerun exact committed sources in a clean workspace;
2. add or execute end-to-end loopback 503 and closed-socket cases without adding remote targets;
3. retain raw result digests and exact source identities;
4. derive measured workload-specific budgets separately;
5. require a later explicit founder gate before any remote/ALPS adapter or paid resource.

No CI rerun, PR, main merge, production probe, deployment, spending or Orchestrator activation is part of this review.
